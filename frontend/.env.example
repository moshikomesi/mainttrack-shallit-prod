# Copy to .env.local for local development (never commit real .env files).
# Production builds use .env.production (see npm run build).

# Local API (Vite dev server proxies /api to Kestrel)
# VITE_API_URL=http://localhost:5062

# Production (ShallIT) — same-origin API under /api
VITE_API_URL=https://mt.shallit.co.il/api
